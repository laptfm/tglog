# tglog
tglog

Откройте Terminal (Cmd+Space → "Terminal" → Enter):
bashpython3 --version
Если видите Python 3.x.x — переходите к шагу 2.
Если "command not found":
bash/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
После установки Homebrew:
bashbrew install python

Шаг 2: Устанавливаем библиотеки
bashpip3 install pyobjc-framework-Cocoa pyobjc-framework-ApplicationServices
Ждём 1-2 минуты.

Шаг 3: Узнаём своё имя пользователя
bashwhoami
Запомните результат, например ivan.

Шаг 4: Создаём папку
bashmkdir -p ~/Library/Application\ Support/TelegramLogger
Эта папка скрыта от обычного просмотра в Finder.

Шаг 5: Создаём скрипт
Скопируйте целиком и вставьте в Terminal:

Делаем исполняемым:
bashchmod +x ~/Library/Application\ Support/TelegramLogger/logger.py

Шаг 6: Разрешение на Accessibility
Это критически важный шаг — без него ничего не работает.

Системные настройки → Конфиденциальность и защита → Accessibility
Нажмите замок 🔒 → введите пароль
Нажмите + → найдите и добавьте Terminal (/Applications/Utilities/Terminal.app)
Убедитесь что стоит галочка ✅


Шаг 7: Тестовый запуск
Сначала проверим что всё работает вручную:
bashpython3 ~/Library/Application\ Support/TelegramLogger/logger.py
```

Откройте Telegram, пощёлкайте по чатам. Должны появляться сообщения в Terminal:
```
[2026-02-18 12:34:56] ✓ messages_2026-02-18_001.txt (0.05/10 МБ)
Если работает — нажмите Ctrl+C и переходите к шагу 8.

Шаг 8: Настройка автозапуска в фоне
Узнайте путь к python3:
bashwhich python3
Запомните результат (обычно /usr/local/bin/python3 или /opt/homebrew/bin/python3).
Создайте файл автозапуска (замените ivan на своё имя из шага 3 и путь к python3 если отличается):

Активируйте:
bashlaunchctl load ~/Library/LaunchAgents/com.user.telegramlogger.plist
Проверьте что запустился:
bashlaunchctl list | grep telegramlogger
Если видите строку с цифрами — работает. Теперь Terminal можно закрыть.









Для автозапуска — добавьте python3

Как добавить оба:
Системные настройки → Конфиденциальность → Accessibility:

Добавьте Terminal: /Applications/Utilities/Terminal.app
Добавьте python3:

Узнайте путь: which python3
Обычно /usr/local/bin/python3 или /opt/homebrew/bin/python3
Нажмите + → Cmd+Shift+G → вставьте путь → добавьте



Обе галочки должны стоять ✅

Проверка:
После настройки LaunchAgent проверьте что работает:
bash# Запустите автозапуск
launchctl load ~/Library/LaunchAgents/com.user.telegramlogger.plist

# Подождите 10 секунд, откройте Telegram

# Проверьте есть ли записи
ls -lh ~/Library/Application\ Support/TelegramLogger/

# Должен появиться файл messages_ДАТА_001.txt
Если файла нет или он пустой — проверьте ошибки:
bashcat ~/Library/Application\ Support/TelegramLogger/daemon_errors.txt
